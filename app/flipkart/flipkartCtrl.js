'use strict';

angular.module('website')
    .controller('flipkartCtrl', ['$scope','$state','$rootScope','$window',
        function ($scope, $state, $rootScope, $window) {
            var self = this;
            self.name = 'flikart';

        }
	]);